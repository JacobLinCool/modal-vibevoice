[project]
name = "modal-vibevoice"
version = "0.1.0"
description = "Modal deployment of Microsoft VibeVoice-ASR"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.0.0",
]

[tool.uv]
package = false
